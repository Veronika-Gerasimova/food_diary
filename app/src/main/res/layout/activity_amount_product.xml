<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F1C9F8"
    android:padding="16dp"
    tools:context=".view.AmountProductActivity">

    <TextView
        android:id="@+id/selected_product_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Выбранный продукт"
        android:textSize="18sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/quantity_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/selected_product_textview"
        android:layout_marginTop="23dp"
        android:layout_marginBottom="16dp"
        android:hint="Количество (в граммах)"
        android:inputType="numberDecimal" />

    <Button
        android:id="@+id/save_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/quantity_edittext"
        android:layout_marginTop="29dp"
        android:onClick="saveProduct"
        android:text="Сохранить" />


</RelativeLayout>
